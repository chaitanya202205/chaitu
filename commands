Event Git for Good
We are running a series of GIT events next week on your off days to ensure every student has a significant GIT profile
 git pull it will update the changes what we did from cloud(global) to local system